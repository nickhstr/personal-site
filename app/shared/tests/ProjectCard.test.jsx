var React = require('react');
var expect = require('expect');
var ProjectCard = require('ProjectCard');

describe('ProjectCard', () => {
	it('should exist', () => {
		expect(ProjectCard).toExist();
	});
});