var React = require('react');
var expect = require('expect');
var FeaturedProjects = require('FeaturedProjects');

describe('FeaturedProjects', () => {
	it('should exist', () => {
		expect(FeaturedProjects).toExist();
	});
});